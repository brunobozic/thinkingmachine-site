[Definition]
# Match 401, 403, or 429 responses logged by Traefik (JSON access log via journald).
# Triggers when a single IP racks up too many auth-failed / rate-limited responses.
failregex = "ClientHost":"<HOST>".*"DownstreamStatus":(401|403|429)
ignoreregex =
